<div class="accordion mb-5 filter" id="accordionExample">
        <div class="card">
            <div class="card-header bg-dark text-center" id="headingThree">
                <h5 class="mb-0">
                    <b><button class="btn btn-link text-light" type="button" data-toggle="collapse" data-target="#collapseThree" 
                    aria-expanded="true" aria-controls="collapseThree" style="text-decoration: none"><i class="fas fa-filter text-light mr-2"></i> FILTRES</b></button>
                </h5>
            </div>
            <div id="collapseThree" class="collapse" aria-labelledby="headingThree" data-parent="#accordionExample">
                <div class="card-body bg-secondary">
{{ form_start(form, {attr: {class: 'filter'}}) }}
    <h4 class="mt-3 p-1 alert alert-primary text-center"><b>TITRE</b></h4>
    <div>{{ form_row(form.q) }}</div>
    <h4 class="mt-3 p-1 alert alert-primary text-center"><b>THEMES</b></h4>
    <div class="text-light">{{ form_row(form.categories) }}</div>
    {# <h4 class="p-1 alert alert-primary text-center"><b>NOTE</b></h4>
    <div class="row d-flex justify-content-around">
        <div class="col-md-6 alert alert-danger">
            {{ form_row(form.min) }}
        </div>
        <div class="col-md-6 alert alert-success">
            {{ form_row(form.max) }}
        </div>
    </div> #}
    <h4 class="p-1 alert alert-primary text-center"><b>AUTEUR</b></h4>
    <div>{{ form_row(form.auteur) }}</div>

<button type="submit" class="btn btn-primary p-3 w-100 mb-5 border-light"><i class="fas fa-filter"></i><b> FILTRER</b></button>

{{ form_end(form) }}

</div>
</div>
</div>
</div>